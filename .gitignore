# Config files with secrets/specifics - commit templates instead
central_server_package/app/sla_config.env
linux_agent_package/agent_config.env
windows_agent_package/agent_config.ps1

# SQLite databases and journals on HOST (if setup script is run in project root by mistake)
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# Log files (if generated in project root by mistake)
*.log

# Docker build context temp files (if any)
central_server_package/app_build_temp/

# IDE/Editor specific files
.vscode/
.idea/
*.swp
*~